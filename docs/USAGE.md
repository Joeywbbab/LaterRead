# 使用指南

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `⌘⇧L` | 保存当前浏览器页面（全局） |
| `⌘P` → "LaterRead" | Obsidian 命令面板 |

---

## 菜单栏 App

### 保存页面

在任意浏览器中按 `⌘⇧L`，自动保存当前页面到 inbox。

支持的浏览器：
- Safari
- Chrome
- Arc

### 查看列表

点击菜单栏 📖 图标，下拉查看待读列表。

### 操作条目

- **打开链接**：点击条目直接在浏览器打开
- **标记已读**：右键条目 → Toggle Read（或点击左侧圆圈）
- **删除条目**：右键条目 → Delete
- **查看摘要**：鼠标悬停在条目上查看 AI 生成的摘要

### 批量分类

点击 "Classify All" 按钮：
- 自动对所有未分类条目进行 AI 分类
- 显示实时进度（每 3 个条目更新一次）
- 自动限流（每次请求间隔 0.5 秒）
- 显示成功/失败统计

### 设置

点击 "Settings" 按钮：
- 配置 OpenRouter API Key
- 查看 Vault 路径
- 测试 API 连接

---

## Obsidian 插件

### 打开侧边栏

方式 1：点击左侧栏 📖 图标
方式 2：命令面板 `⌘P` → 搜索 "LaterRead"

### 手动添加条目

1. 点击 `+ 添加` 按钮
2. 输入 URL（自动读取剪贴板）
3. 选择分类或使用 AI 自动分类
4. 添加备注（可选）
5. 点击保存

### 管理条目

- **查看详情**：点击条目展开查看完整摘要和备注
- **标记已读**：点击右侧 `✓ 已读` 按钮
- **删除**：点击右侧 `✕` 按钮
- **编辑**：直接在 `inbox.md` 中编辑

### 生成周末 Digest

1. 点击 `📋 生成 Digest` 按钮
2. 自动生成本周阅读清单到 `【00】LaterRead/YYYY-WXX.md`
3. 包含分类统计、重点条目、阅读笔记

### 归档

已读条目会自动归档到 `【00】LaterRead/archive.md`，按月份分组。

---

## 分类系统

### 自动分类

AI 会根据标题、URL、域名自动判断文章类别：

| 分类 | 判断依据 |
|------|----------|
| 🤖 AI/Tech | 包含 AI、LLM、GPT、机器学习等关键词 |
| 🛠️ Dev Tools | 编程、开发工具、IDE、框架相关 |
| 📦 Product | 产品发布、SaaS、创业公司 |
| 🎨 Design | UI/UX、设计系统、Figma |
| 💼 Business | 创业、投资、增长、营销 |
| 📚 Research | 学术论文、研究方法 |
| 🎯 Career | 求职、面试、职业发展 |
| ⚡ Productivity | 效率工具、工作流优化 |
| 📖 Reading | 书籍、长文、写作 |
| 📌 General | 其他未分类内容 |

### 手动修改分类

直接在 `inbox.md` 中修改 emoji 即可：

```markdown
- [ ] 🤖 [文章标题](url) | domain | 2025-01-12
```

改成：

```markdown
- [ ] 📦 [文章标题](url) | domain | 2025-01-12
```

---

## 数据格式

### inbox.md 格式

```markdown
# 📖 LaterRead Inbox

## 🤖 AI/Tech

- [ ] 🤖 [OpenAI GPT-5 发布](https://example.com) | example.com | 2025-01-12
>  OpenAI 正式发布 GPT-5，性能提升 10 倍
> 📝 重要！需要研究对产品的影响

## 🛠️ Dev Tools

- [x] 🛠️ [VS Code 新特性](https://code.visualstudio.com) | code.visualstudio.com | 2025-01-11
>  VS Code 1.95 支持原生 AI 补全
```

### 字段说明

- `[ ]` / `[x]`：未读 / 已读
- Emoji：分类标识
- `[标题](URL)`：文章标题和链接
- `domain`：来源域名
- `日期`：保存日期
- `>  摘要`：AI 生成的中文摘要
- `> 📝 备注`：用户添加的备注

---

## 自定义配置

### 修改 Vault 路径

编辑 `laterread-mvp/menubar-app/LaterRead/Config.swift`:

```swift
static let vaultName = "YourVaultName"
static let inboxRelativePath = "【00】LaterRead/inbox.md"
```

### 更换 AI 模型

编辑 `Config.swift`:

```swift
static let aiModel = "google/gemini-2.5-flash-preview-05-20"
```

支持的模型见 [OpenRouter Models](https://openrouter.ai/docs/models)

### 添加新分类

编辑 `laterread-mvp/menubar-app/LaterRead/CategoryManager.swift`:

```swift
static var categories = [
    "new-category": ("🎉", "New Category", "keyword1, keyword2"),
    ...
]
```

记得同时更新 `categoryOrder` 数组。

### 开机启动

系统设置 → 通用 → 登录项 → 添加 `LaterRead.app`

---

## 工作流建议

### 日常使用

1. 浏览网页时遇到感兴趣的文章，按 `⌘⇧L` 保存
2. AI 自动分类和生成摘要
3. 空闲时点击菜单栏查看待读列表
4. 点击条目在浏览器中阅读
5. 阅读后右键标记已读

### 周末整理

1. 打开 Obsidian 侧边栏
2. 点击 `📋 生成 Digest` 生成本周阅读清单
3. 回顾本周阅读的内容
4. 整理笔记和想法

### 批量处理

1. 一次性保存多个网页（连续按 `⌘⇧L`）
2. 点击 "Classify All" 一键批量分类
3. 查看分类结果，手动调整不准确的分类
4. 按分类阅读，提高效率
